# Worker Configuration

# Vercel API settings
vercel_api_url: "https://life-is-short-landing.vercel.app/api"
worker_token: "i39dg0edinw0d98ghjwlekigfhjuwe0dignjwopdi9ghslaiq"

# Worker identification
worker_id: "gpu-worker-001"

# Polling settings
polling_interval: 5  # seconds to wait when no task is available
api_timeout: 30  # seconds for API requests
lease_duration_seconds: 600  # 10 minutes - how long worker holds a task
heartbeat_interval: 120  # 2 minutes - how often to send heartbeat

# Paths
wan_repo_path: "./Wan2.2"
model_path: "./Wan2.2/Wan2.2-TI2V-5B"
temp_dir: "./temp"
log_dir: "./logs"

# Inference settings
task_type: "ti2v-5B"
video_size: "1280*704"  # Output video size. Wan2.2 automatically adjusts input image to this size
frame_num: 121  # 4n+1 (default: 121, can be overridden per task via API)
sample_solver: "unipc"
sample_steps: 50
cfg_scale: 7.5

# Retry settings
max_retries: 3
retry_delay: 5  # seconds

# Cleanup
auto_cleanup_temp: true
